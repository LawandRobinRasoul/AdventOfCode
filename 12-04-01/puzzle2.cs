using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace _12_04_01
{
    public class puzzle2
    {
        //  string[] first;
        // string [] second;
        // string[] pairs;
        // List<int> firstNumbers = new List<int>();
        // List<int> secondNumbers = new List<int>();
        // int totalSum = 0;
        // bool containing = false;
        // string[] lines = File.ReadAllLines("C:\\AdventCode\\12-04-01\\input4.txt");


        // foreach (string line in lines)
        // {
        //    pairs = line.Split(",");
        //    first = pairs[0].Split("-");
        //    second = pairs[1].Split("-");
          
        //    for(int i = int.Parse(first[0]); i <= int.Parse(first[1]); i++)
        //    {
        //      firstNumbers.Add(i);
        //    }
        //     for(int i = int.Parse(second[0]); i <= int.Parse(second[1]); i++)
        //    {
        //      secondNumbers.Add(i);
        //    }

        //    foreach(var number in firstNumbers)
        //    {
        //       if(secondNumbers.Contains(number))
        //       {
        //         containing = true;
        //       }
        //    }
          
        //   if(containing == true)
        //   {
        //     totalSum += 1;
        //   }
        
        //   containing = false;
        //   firstNumbers.Clear();
        //   secondNumbers.Clear();


        // }

        // Console.WriteLine(totalSum);
    }
}